/**
 * In-process Rift engine over the Panama Foreign Function &amp; Memory API (stable, JDK 22+).
 * The {@code librift_ffi} C-ABI v2 binding and {@code Rift.embedded()} wiring arrive with the
 * embedded issues; this module currently establishes the artifact and its JDK 22 build.
 */
package io.github.achirdlabs.rift.embedded;
