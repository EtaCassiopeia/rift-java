package io.github.etacassiopeia.rift;

/**
 * Placeholder configuration for {@link Rift#spawn(SpawnOptions)}. The spawn transport (launching
 * and managing a local engine process) arrives in issue #5; until then {@code Rift.spawn(...)}
 * always throws.
 */
public final class SpawnOptions {

    public SpawnOptions() {
    }
}
