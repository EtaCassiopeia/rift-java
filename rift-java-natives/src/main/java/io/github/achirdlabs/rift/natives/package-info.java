/**
 * Per-platform classifier jars bundling the {@code librift_ffi} cdylib. The download, SHA-256
 * verification, and classifier packaging pipeline is added in issue #9; this module currently
 * reserves the {@code native/&lt;os&gt;-&lt;arch&gt;/} classpath layout the embedded resolver reads.
 */
package io.github.achirdlabs.rift.natives;
