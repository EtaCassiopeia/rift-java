/**
 * Optional Jackson POJO body codec for rift-java. The codec surface arrives with the wire model
 * and DSL; this module currently establishes the artifact and its Jackson dependency.
 */
package io.github.achirdlabs.rift.jackson;
